{
  "name": "metering-plugins-workspace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "metering",
    "metering-backend"
  ],
  "scripts": {
    "build:all": "yarn workspaces foreach -A run build",
    "tsc:all": "yarn workspaces foreach -A run tsc",
    "test:all": "yarn workspaces foreach -A run test",
    "lint:all": "yarn workspaces foreach -A run lint"
  },
  "devDependencies": {
    "@backstage/cli-defaults": "^0.1.3",
    "@jest/environment-jsdom-abstract": "^30.4.1",
    "jest": "^30.0.0",
    "jest-environment-jsdom": "^30.4.1",
    "typescript": "~5.7.0"
  },
  "packageManager": "yarn@4.8.1",
  "engines": {
    "node": "22 || 24"
  }
}
