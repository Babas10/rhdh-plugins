{
  "name": "@internal/backstage-plugin-todo-backend",
  "version": "0.1.0",
  "description": "Dynamic plugin wrapper for @backstage-community/plugin-todo-backend",
  "private": true,
  "backstage": {
    "role": "backend-plugin"
  },
  "scripts": {
    "build": "backstage-cli package build",
    "build:types": "tsc --declaration --emitDeclarationOnly",
    "tsc": "backstage-cli package lint",
    "lint": "backstage-cli package lint",
    "clean": "backstage-cli package clean"
  },
  "dependencies": {
    "@backstage-community/plugin-todo-backend": "^0.24.0"
  },
  "devDependencies": {
    "@backstage/cli": "^0.36.3",
    "typescript": "~5.7.0"
  },
  "files": [
    "dist",
    "dist-dynamic"
  ],
  "main": "dist/index.cjs.js",
  "types": "dist/index.d.ts"
}
